import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // Several existing components reset derived state (quote, availability,
      // menu-open) in a useEffect keyed off props/pathname changes. That's a
      // real anti-pattern per the React docs, but fixing it means restructuring
      // each component individually - tracked separately, not blocking CI on
      // pre-existing code this pass didn't touch.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
